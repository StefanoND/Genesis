# Config

For systems that use some form of scene/node-based configuration.

Making scene-based config files allows for more flexibility in how the
configuration works, plus allows using the Editor's UI to manage the
configuration data, which is better when visual presentation are needed.

Some directories that might be used are:

- system1Config
- system2Config
- system3Config
